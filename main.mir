// todo: add match statement struct destructuring (struct pattern matching), pipe operator

enum Shape {
    Rectangle { length, width },
    Square { side },
    Circle { radius },
}

let circle = Shape::Circle { radius = 4 }

match circle {
    Shape::Circle { radius } -> {
        PI * r ^ 2
    }
}